//import Shapes from each file
const circle = require("./circle");
const square = require("./square");
const triangle = require("./triangle");

// create generate shape function to generate each one
class Shape {
    constructor () {
        this.textColor = "";
        this.text = "";
        this.shape = shape;
        this.shapeColor = "";
    };
    };

class circle extends Shape {
    constructor(textColor,text,shape,shapeColor) {
    super(textColor,text,shape,shapeColor);
};
};
class square extends Shape {
    constructor(textColor,text,shape,shapeColor) {
    super(textColor,text,shape,shapeColor);
    
};
};
class circle extends Shape {
    constructor(textColor,text,shape,shapeColor) {
    super(textColor,text,shape,shapeColor);
    
};
};

function generateShape (data); {
    if (shape === "circle") {
        return <circle cx="25" cy="75" r="20"/>
    }
    if (shape === "square") {
        return
    }
    if (shape === "triangle") {
        return
    }
};

// export function to the index.js file
module.exports(shape);